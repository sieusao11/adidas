<%= form_with model: @product, local: true do |form| %>

<% if @product.errors.any? %>
<div id="error_explanation">
  <h2>
    <%= pluralize(@product.errors.count, "error") %> prohibited
    this product from being saved:
  </h2>
  <ul>
    <% @product.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>

<%= form.label :productname %><%= form.text_area :productname %>
<%= form.label :productimagepath %><%= form.text_area :productimagepath %>
<%= form.label :gender_id %><%= form.text_area :gender_id %>
<%= form.label :age_id %><%= form.text_area :age_id %>
<%= form.label :price %><%= form.text_area :price %>
<%= form.label :originalpride %><%= form.text_area :originalpride %>
<%= form.label :color_id %><%= form.text_area :color_id %>
<%= form.label :colordetail %><%= form.text_area :colordetail %>
<%= form.label :franchise_id %><%= form.text_area :franchise_id %>
<%= form.label :producttype_id %><%= form.text_area :producttype_id %>
<%= form.label :brand_id %><%= form.text_area :brand_id %>
<%= form.label :category_id %><%= form.text_area :category_id %>
<%= form.label :sport_id %><%= form.text_area :sport_id %>
<%= form.label :productdetailspath %><%= form.text_area :productdetailspath %>


<%= form.submit :'Cập Nhật Sản Phẩm' %>

<% end %>

<!-- #bin/rails generate model Product 
productname:text 
productimagepath:text 
gender_id:text 
age_id:text 
price:integer 
originalpride:integer 
color_id:text 
colordetail:text 
franchise_id:text 
producttype_id:text 
brand_id:text 
category_id:text 
sport_id:text 
productdetailspath:text  -->